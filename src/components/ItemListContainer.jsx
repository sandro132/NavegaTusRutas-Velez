const ItemListContainer = ({ mensaje }) => {
    return (
        <div className="hero">
            <h2>{mensaje}</h2>
            <p>
                Tecnología gamer ⚔️ · Componentes · Servicios técnicos especializados
            </p>
        </div>
    );
};

export default ItemListContainer;